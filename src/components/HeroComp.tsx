import { Box } from '@chakra-ui/react'
import { HeroCarousel } from '../pages'

const HeroComp = () => {
  return (
    <Box pt={['3rem','6rem', '8rem']}>
       <HeroCarousel /> 
    </Box>
  )
}

export default HeroComp
